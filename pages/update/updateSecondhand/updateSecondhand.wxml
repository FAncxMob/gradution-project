<view id="releaseLegwork">
    <view class='login-container setBorder twoSideShadow'>
        <form class="login-form " bindsubmit="submitForm">
            <view class="{{showPreview?hideForm:''}}">
                <view class="section">
                    <view class="section-title">标题 <text class="red-1">*</text></view>
                    <textarea value="{{detail.title}}" show-confirm-bar="{{false}}" name="title"
                        placeholder-class="input" auto-height placeholder="请输入标题" />
                    </view>
                <view class="section">
                    <view class="section-title">物品详情介绍  <text class="red-1">*</text></view>
                    <textarea  value="{{detail.desc}}"  show-confirm-bar="{{false}}"  name="desc" placeholder-class="input" auto-height placeholder="品牌型号，新旧程度，入手渠道，转手原因..." />
                </view>
                <view class="section">
                    <view class="section-title">出售价格(元) <text class="red-1">*</text></view>
                    <textarea  value="{{detail.price}}"  show-confirm-bar="{{false}}" placeholder-class="input" name="price" auto-height placeholder="请输入出售价格" />
                </view>
                <view class="section">
                    <view class="section-title">入手原价(元) <text class="red-1">*</text></view>
                    <textarea value="{{detail.invitationsDetail.buyPrice}}"  show-confirm-bar="{{false}}" placeholder-class="input" name="buyPrice" auto-height placeholder="请输入入手原价" />
                </view>
                <view class="section">
                    <view class="section-title">物品类别</view>
                    <textarea  value="{{detail.invitationsDetail.tag}}" show-confirm-bar="{{false}}"  placeholder-class="input" name="tag" auto-height placeholder="如电子商品，学习用品，生活用品等" />
                </view>
                <view class="section">
                    <view class="section-title">配送方式  <text class="red-1">*</text></view>
                    <picker class="borderBottom" bindchange="bindPickerChange" value="{{deliveryIndex}}"
                    range="{{deliveryArray}}">
                    <view class="picker">
                        {{deliveryArray[deliveryIndex]}}
                    </view>
                    </picker>
                </view>
            </view>
          <block wx:if="{{deliveryIndex!=2}}">
            <view class="section">
                <view class="section-title">提货地址 <text class="red-1">*</text></view>
                <view bindtap="chooseAddress" class="address text-ellipsis-row2">
                    <block wx:if="{{addressData._id}}">
                        <view>姓名：{{addressData.name}}</view>
                        <view>电话：{{addressData.tel}}</view>
                        <view class="text-ellipsis-row3">详细地址：{{addressData.location}}</view>
                    </block>
                    <block wx:else>
                        请选择提货地址
                    </block>
                </view>
            </view>
          </block>
             <!-- 为了让图片的高度等于高度，利用padding垂直的百分比是按照父元素的宽度来计算来进行 -->
            <view class="section imageBx">
                <view class="section-title">选择图片  <text class="red-1">*</text></view>
                <text class="mb10 fz26">您最多选择{{maxLength}}张图片!</text>
                <block wx:if="{{realPath.length}}">
                    <view  class="choosePicBox" > 
                        <view class="imgWrapper mb20 por" bindtap="previewImage" data-index="{{index}}" wx:for="{{realPath}}" wx:key="{{index}}">
                            <image mode="cover"  src="{{item}}">
                            </image>
                            <block wx:if="{{realPath[index]}}">
                                <text class="iconfont  icon-chenggong opa-8 setCenter green-1 fz80 z-index-10"></text>
                            </block>
                            <block wx:else>
                                <text class="iconfont icon-shibai2 opa-8 setCenter red-1 fz80 z-index-10"></text>
                            </block>
                            
                         </view>
                         <view bindtap="choosePic" class="imgWrapper ">
                            <image mode="cover"  class="adjust setBorder gray-1 fz30 p30" src="/icon/plus.png"></image>
                         </view>
                    </view>
                </block>
                <block wx:else>
                    <view  bindtap="choosePic" class="picIcon setBorder df align-center justify-center">
                        <text class=" fwb fz100 iconfont icon-tianjia"></text>
                    </view>
                </block>
                <text class="mb10 fz26">选最棒的图片作为主图再发布，附上购买证明，商品更容易被卖出哦~</text>
            </view>
           
            <view class="btn-area">
                <button type="primary" formType="submit"> 提交修改
                </button>
            </view>
        </form>
    </view>

    <mp-gallery bind:hide="hide" hide-on-click="{{true}}" show-delete="{{true}}" show="{{showPreview}}"
    binddelete="deletePic" img-urls="{{previewImageUrls}}" current="{{previewCurrent}}"></mp-gallery>
</view>