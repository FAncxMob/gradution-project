<view class="">
    <view class="">
        <swiper class="bottomShadow" indicator-dots="{{true}}" autoplay="{{ true }}" circular="{{ true }}"
            interval="{{ 2000 }}" duration="{{ 500 }}" previous-margin="{{ 0 }}px" next-margin="{{ 0 }}px">
            <block wx:for="{{ background }}" wx:key="*this">
                <swiper-item>
                    <image mode="aspectFit   " class="swiper-pic" src="{{ item }}"></image>
                </swiper-item>
            </block>
        </swiper>
    </view>
    <SchoolNewsList data="{{schoolNews}}" title="{{noDataText}}" haveOperation="{{false}}" noDataHeight="500rpx">
    </SchoolNewsList>


</view>